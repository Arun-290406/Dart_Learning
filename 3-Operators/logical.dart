
void main()
{
  int age=50;
  if(age >= 18 && age<=80) //&& - both condition satisfy, ||--satisfy any one cond 
  {
    print("$age is eligible for Vote");
  }
  else
  {
     print("$age is not eligible for Vote"); 
  }
}